// No-op: Skybridge ui.domain hash is correct by default.
// Claude Web hashes the full connector URL (including /mcp).
// Skybridge also hashes the full URL. No patch needed.
console.log("skybridge patch: no-op (hash is correct by default)");
